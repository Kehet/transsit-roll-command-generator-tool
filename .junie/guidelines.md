# Terminal
This project runs inside WSL (Ubuntu). Always use Linux/Bash commands, not PowerShell.
Use forward slashes `/` for paths. The project root is at `/home/kehet/Code/transsit-dice-roller-tool/`.
